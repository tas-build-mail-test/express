---
framework: mocha  
preRun:
  command:
    - npm install  
preMerge:
  pattern:
    - "./test/acceptance/*.js"
postMerge:
  pattern: 
    - "./test/acceptance/*.js"
postRun:
  command:
    - node --version
configFile: mocharc.yml